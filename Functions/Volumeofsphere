public class Solution {
    // DO NOT MODIFY THE ARGUMENTS WITH "final" PREFIX. IT IS READ ONLY
    public int solve(final int A) {
        double volume = (4 * Math.PI * A * A * A)/3;
        int res = (int)Math.ceil(volume);
        return res;
    }
}